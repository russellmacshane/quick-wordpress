Docker Setup
============

docker build -t quick-wordpress .
docker run --rm -p 80:80 quick-wordpress

docker exec -it {container} /bin/bash
docker exec -it {container} qwcli

docker stop {container}

Docker Compose Setup
===================

docker-compose up -d
docker-compose down 

docker-compose stop
docker-compose start

docker-compose exec qwp /bin/bash
docker-compose exec qwp qwcli
